// services/googleSheetService.ts

/**
 * @deprecated
 * 이 서비스는 더 이상 사용되지 않습니다.
 * 데이터는 이제 빌드 시점에 `scripts/sync-data.ts` 스크립트를 통해
 * Google Sheet에서 가져와 정적 JSON 파일로 생성됩니다.
 * `context/DataContext.tsx`에서 로컬 JSON 파일을 직접 로드하여 사용합니다.
 */

export {};
